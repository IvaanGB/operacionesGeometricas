<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="geometria.ivaangb.com.operacionesgeometricas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Principal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".lvArea" />
        <activity android:name=".areaCuadrado" />
        <activity android:name=".areaRectangulo" />
        <activity android:name=".areaCirculo" />
        <activity android:name=".areaTriang" />
        <activity android:name=".lvVolumen" />
        <activity android:name=".volEsfera" />
        <activity android:name=".volCilindro" />
        <activity android:name=".volCono" />
        <activity android:name=".volCubo" />
        <activity android:name=".opRealizadas"></activity>
    </application>

</manifest>